User page:
	Cancel request

Update database;
	Add user level: admin, mod, normal user, banned user, unactivated user.

Build Admin Page:
	Add user control: ban, re-activate, make mod.
	View statistics:	user statistics, request statistics => send as announcements.

Add option:$archive_request
	If yes:	save paper on server
	If No:	delete paper on server
	
